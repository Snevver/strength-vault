# Example environment variables for local development
# Copy this file to .env and fill in the real values (do NOT commit .env)

# Your Supabase project URL (e.g. https://xyzcompany.supabase.co)
VITE_SUPABASE_URL=

# Your Supabase anon (publishable) key — found under Project -> Settings -> API -> anon/public
VITE_SUPABASE_PUBLISHABLE_KEY=

# Optional: if you use other env vars for auth or serverless functions, add them here

# Example PowerShell usage (temporary for current session):
# $env:VITE_SUPABASE_URL='https://your-project-ref.supabase.co'; $env:VITE_SUPABASE_PUBLISHABLE_KEY='your-anon-key'; npm run dev
